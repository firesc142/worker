# Allow install scripts for native binary packages (required for npm v11+)
# koffi: downloads prebuilt Windows native binaries for screen/input capture
# cloudflared: downloads the cloudflared tunnel binary
allow-scripts=koffi:true,cloudflared:true
